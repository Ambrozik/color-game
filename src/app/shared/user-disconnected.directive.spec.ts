import { UserDisconnectedDirective } from './user-disconnected.directive';

describe('UserDisconnectedDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDisconnectedDirective();
    expect(directive).toBeTruthy();
  });
});
