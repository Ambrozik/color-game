import { UserConnectedDirective } from './user-connected.directive';

describe('UserConnectedDirective', () => {
  it('should create an instance', () => {
    const directive = new UserConnectedDirective();
    expect(directive).toBeTruthy();
  });
});
